~   用户的home目录
$*  接收所有参数，合并成一个字符串
$@  接收所有参数，不合并
$#  接收参数的个数
$?  上一个命令执行的结果状态码，0为成功，其他为失败
\   连接两行，如果脚本一行的字符太多，可以通过\两行脚本
$0  要执行的shell的文件名
$1  第一个参数（shell的位置参数最多可以接收10个参数，即0～9，可以通过使用shift将参数左移一位来取得10以后的参数）